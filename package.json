{
  "activationEvents": [
    "onLanguage:typescript",
    "onLanguage:javascript",
    "onLanguage:typescriptreact",
    "onLanguage:javascriptreact"
  ],
  "categories": [
    "Formatters",
    "Other"
  ],
  "contributes": {
    "commands": [
      {
        "category": "Sort Code",
        "command": "sortCodePlugin.sortCurrentFile",
        "title": "Sort Functions in Current File"
      }
    ],
    "keybindings": [
      {
        "command": "sortCodePlugin.sortCurrentFile",
        "key": "ctrl+m ctrl+z",
        "mac": "cmd+m cmd+z",
        "when": "editorTextFocus && (editorLangId == typescript || editorLangId == javascript || editorLangId == typescriptreact || editorLangId == javascriptreact)"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "sortCodePlugin.sortCurrentFile",
          "when": "editorLangId == typescript || editorLangId == javascript || editorLangId == typescriptreact || editorLangId == javascriptreact || resourceExtname =~ /\\.(ts|js|tsx|jsx|mjs|cjs|mts|cts)$/"
        }
      ],
      "editor/context": [
        {
          "command": "sortCodePlugin.sortCurrentFile",
          "group": "1_modification",
          "when": "resourceExtname =~ /\\.(ts|js|tsx|jsx|mjs|cjs|mts|cts)$/"
        }
      ]
    }
  },
  "description": "Sort TypeScript and JavaScript functions alphabetically with public functions on top. Supports .ts, .js, .tsx, .jsx, .mjs, .cjs, .mts, .cts files",
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^8.35.0",
    "@typescript-eslint/parser": "^8.35.0",
    "esbuild": "^0.25.6",
    "eslint": "^9.29.0",
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-sort-keys-fix": "^1.1.2",
    "prettier": "^3.6.1",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.2",
    "typescript": "^4.9.4"
  },
  "displayName": "Sort Code Functions",
  "engines": {
    "vscode": "^1.74.0"
  },
  "icon": "icon.png",
  "main": "./dist/extension.js",
  "name": "sort-code-plugin",
  "publisher": "salammorcos",
  "repository": {
    "type": "git",
    "url": "https://github.com/all4peace/sort-code-plugin"
  },
  "scripts": {
    "build": "npm run clean && node esbuild.mjs",
    "build:dev": "npm run clean && node esbuild.mjs",
    "build:prod": "npm run clean && node esbuild.mjs --production",
    "clean": "rimraf ./dist",
    "compile": "tsc -p ./",
    "lint": "eslint src --ext ts",
    "pretest": "npm run compile && npm run lint",
    "test": "tsx src/test/runTest.ts",
    "vscode:package": "npm run build:prod && vsce package",
    "vscode:prepublish": "npm run build:prod",
    "watch": "node esbuild.mjs --watch"
  },
  "version": "1.1.3"
}
